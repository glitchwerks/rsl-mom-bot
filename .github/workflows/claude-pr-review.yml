name: Claude PR Review

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  packages: read
  pull-requests: write
  statuses: write

jobs:
  review:
    uses: glitchwerks/github-actions/.github/workflows/claude-pr-review.yml@31e162d256ada38e09af0c2123637f5de238d1ef  # v2.6.2
    with:
      authorized_users: cbeaulieu-gt
    secrets: inherit
