name: Claude

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

permissions:
  contents: write
  issues: write
  packages: read
  pull-requests: write

jobs:
  respond:
    uses: glitchwerks/github-actions/.github/workflows/claude-tag-respond.yml@31e162d256ada38e09af0c2123637f5de238d1ef  # v2.6.2
    with:
      authorized_users: cbeaulieu-gt
    secrets: inherit
