name: Claude CI Fix

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

permissions:
  contents: write
  pull-requests: write

jobs:
  fix:
    uses: glitchwerks/github-actions/.github/workflows/ci-failure.yaml@31e162d256ada38e09af0c2123637f5de238d1ef  # v2.6.2
    secrets: inherit
    with:
      auto_apply: true
